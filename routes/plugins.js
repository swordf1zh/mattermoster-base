const pluginsRouter = require('express').Router();

/**
 * ROUTE PLUGINS
 * Require plugins here and set routes for them.
 * Eg.:
 * const mattermosterTodo = require('mattermoster-todo-plugin');
 * pluginsRouter.use('/todo', mattermosterTodo);
 */

module.exports = pluginsRouter;
